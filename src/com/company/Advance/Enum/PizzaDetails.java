package com.company.Advance.Enum;

enum PizzaSize {
    SMALL, MEDIUM, LARGE

}
enum PizzaName{
    MARGARETA , VEGGIE, PEPPERONI
}
//What is syntax of enum?
// in a java class we can define  enum enum_name and in curly bracket all constant values.

// What is enum?
// A enum is a special "class" that represent a group of constant values like if we use pizza size small medium large here we use enum.

// Why we use enum?
// For constant values or unchangeable values like pizza size.